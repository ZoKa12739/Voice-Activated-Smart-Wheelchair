## 🗂️ 功能改进待办清单

### ☆ 新增项
    - [ ]  软件加固：为 main.c 增加 ADC 中位自校准算法。鉴于不同摇杆中点电压可能有微小偏差，在开机前 1 秒自动采样 50 次取平均值作为 JOY_CENTER，提高精准度。


### 🔴 高优先级

-  **1. 紧急停止物理按键**
    
    - [ ]  在树莓派 GPIO 17（BCM）接一个常开按键（另一端接 GND，内部上拉）
    - [ ]  用 `RPi.GPIO.add_event_detect(pin, GPIO.FALLING, callback=..., bouncetime=300)` 注册中断回调
    - [ ]  回调函数内向串口发送 `'S'` 指令（直接调用 `ser.write(b'S')` 或注入消息队列）
    - [ ]  在程序退出时调用 `GPIO.cleanup()` 释放资源
-  **2. 小程序一键模式切换**
    
    - [ ]  在 OneNET 物模型中新增 `ControlMode`（enum 类型：`voice` / `head`）属性
    - [ ]  在 `weixin_app/pages/control/control.uvue` 新增两个圆角按钮：「语音模式」和「头控模式」
    - [ ]  按钮点击后通过 `uni.request` 向 OneNET 下发 `ControlMode=MODE_VOICE` / `MODE_HEAD`
    - [ ]  树莓派端（`onenet_mqtt_final.py`）在 `on_message` 中解析 `ControlMode` 字段并切换控制模式
-  **3. 系统状态上报到小程序**
    
    - [ ]  在 `onenet_mqtt_final.py` 中新增定时器（每 5 秒），上报 `ControlMode` 和 `SystemStatus`（`online`/`idle`/`sleeping`）
    - [ ]  在 `control.uvue` 的 `getDeviceStatus` 中读取这两个字段
    - [ ]  在页面顶部增加状态指示灯：绿色 = online，橙色 = idle/sleeping，灰色 = 离线

---

### 🟡 中优先级

-  **4. 语音指令 TTS 播报**
    
    - [ ]  安装 `pyttsx3`（`pip install pyttsx3`）
    - [ ]  在 `vosk_v2.py` 的模式切换分支处调用 `engine.say("已切换至头部控制模式"); engine.runAndWait()`
    - [ ]  可选：预录 MP3 文件，用 `pygame.mixer.music.load/play` 播放（延迟更低）
-  **5. 小程序操作历史记录页**
    
    - [ ]  新建 `weixin_app/pages/history/history.uvue` 页面
    - [ ]  在 `pages.json` 注册该页面，并在 `tabBar.list` 中加入「记录」标签
    - [ ]  在 `control.uvue` 的 `sendDirection` 函数内，每次发送时用 `uni.setStorageSync('op_history', ...)` 追加一条 `{cmd, time}` 记录（最多保留 20 条）
    - [ ]  `history.uvue` 用 `uni.getStorageSync('op_history')` 读取并展示列表
-  **6. 头部姿态置信度实时展示（Qt）**
    
    - [ ]  在 `Pi_opencv/mainwindow.h` 中新增 `QProgressBar *confidenceBar`、`QLabel *poseNameLabel` 成员
    - [ ]  在 `mainwindow.cpp` 构造函数中创建并加入布局（放在方向键下方）
    - [ ]  在 `onInferenceFinished` 槽函数中调用 `poseNameLabel->setText(...)` 和 `confidenceBar->setValue(int(confidence*100))` 更新显示

---

### 🟢 低优先级

-  **7. 断线自动重连 + 指数退避**
    
    - [ ]  在 `onenet_mqtt_final.py` 中新增 `on_disconnect` 回调
    - [ ]  回调中用循环重连：最多 5 次，每次间隔 `1s → 2s → 4s → 8s → 16s`（`time.sleep(2**attempt)`）
    - [ ]  每次重连打印日志，超过最大次数后打印告警并停止重连
-  **8. 配置文件化**
    
    - [ ]  新建 `config.py`，将 `vosk_v2.py` 中的 `COMMAND_COOLDOWN`、`ACTIVE_TIMEOUT`、`FULL_RESULT_THRESHOLD`、`SERIAL_PORT`、`BAUD_RATE` 等常量移入
    - [ ]  `vosk_v2.py` 和 `onenet_mqtt_final.py` 改为 `from config import ...` 引用
    - [ ]  可选：改为 `config.json` + `json.load`，无需重新打包即可现场调参
-  **9. 小程序手机端语音控制（可选）**
    
    - [ ]  在 `weixin_app/pages/` 下新建 `voice/voice.uvue` 页面
    - [ ]  使用 `wx.getRecorderManager()` 录音，将音频 base64 上传到后端或直接匹配关键词
    - [ ]  匹配到「前进」/「停止」等词后，调用现有 `sendDirection` 函数下发 OneNET 指令